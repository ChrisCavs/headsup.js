export default (selector) => {

  const header = document.querySelector(selector)
  let prev = window.pageYOffset

  window.addEventListener('scroll', _ => {
    const curent = window.pageYOffset
    
  })
}